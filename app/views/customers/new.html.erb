<h1>Please wright down your information</h1>
<%= form_for(@customer) do |c| %>
  name<%= c.text_field :name %>
  email<%= c.email_field :email %>
  <!-- if文で書け -->
  confirm-email<%= c.email_field :email %>
  password<%= c.password_field :password %>


  <%= c.submit "send" %>
<% end %>
